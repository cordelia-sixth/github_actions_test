# test
name: develop

on:
  push:
    branches: [ develop ]

jobs:
  test:
    runs-on: ubuntu-latest
  
    steps:
      - uses: actions/checkout@v2
      - name: Check branch
      - run: git status
      - run: git branch